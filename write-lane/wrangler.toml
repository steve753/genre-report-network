name = "genre-write"
main = "index.js"
compatibility_date = "2026-08-01"
workers_dev = true

# Secrets (set via `npx wrangler@4 secret put <NAME>` from this directory,
# or in the dashboard followed by Promote version):
#   SUPABASE_URL          — https://mybnrqouoqavmbaywzoa.supabase.co
#   SUPABASE_SERVICE_KEY  — service role key (Steve's alone)
#   WRITE_LANE_TOKEN      — long random string; rides in the connector URL
